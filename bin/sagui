#!/usr/bin/env node

if (process.env.SAGUI_LINK === 'true') {
  require('babel-register')({
    only: /(sagui\/src)/
  })

  require('../src/cli').default(process.argv)
} else {
  require('../lib/cli').default(process.argv)
}
